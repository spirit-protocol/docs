# Archive

Pages moved out of the published site on 2026-07-07 ("working from scratch"
docs reset). They describe the original technical lane — the first
SpiritRegistry contract (being replaced), self-serve wallet registration
(registration is now platform-only), the SDK packages that wrap the old
contract, per-agent discovery endpoints not yet served, and the Spirit Index
registration flow.

Kept for reference and salvage. Anything restored from here must be
re-verified against the current platform and the new registry before it
returns to the nav.
